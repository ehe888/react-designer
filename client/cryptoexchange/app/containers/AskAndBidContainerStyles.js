// AskAndBidContainerStyles.js

export default {
    main: {
        position: 'absolute',
        left: 0,
        top: 0,
        height: '100%',
        // backgroundColor: 'rgb(28, 28, 29)',
        /* reset pointerEvents back to normal, 
           because the parent container is set to be transparent to event */
        color: 'black',
        borderLeft: '1px solid rgba(0,0,0,0.1)',
    }
}